# 로컬 전용 설계·메모 — 공개 레포에 올리지 않음 (docs/는 공개)
notes/

# 데이터·인덱스 (fetch/ingest로 재생성)
data/
.chroma/
*.bm25.pkl

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# env
.env

# 실행 로그·이전 프롬프트 버전 결과 — 공개 안 함
results/logs/
results/_v1_sentence_prompt/
results/_v2_prompt_examples/

# 테스트 fixture 바이너리는 생성 스크립트로 만든다 (make fixtures)
tests/fixtures/*.pdf
tests/fixtures/*.hwpx

# 문항별 결과(jsonl)는 KorQuAD 원문(CC BY-ND)을 포함하므로 레포에 넣지 않는다 — make eval 로 재생성
results/**/*.jsonl
